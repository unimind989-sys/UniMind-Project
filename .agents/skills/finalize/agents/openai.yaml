interface:
  display_name: "Finalize"
  short_description: "Autonomously verify, merge, release, and clean up"
  default_prompt: "Use $finalize to autonomously review, verify, merge to main, release affected zero-cost services when needed, and delete the merged task branch. Ask me only before real-money exposure."

policy:
  allow_implicit_invocation: false
