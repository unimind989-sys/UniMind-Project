# Task record: WP00-T10 revalidate actual Codex tooling and authentication stack

**Task ID:** WP00-T10

**Status:** [~]

**Outcome:** UniMind retains the smallest reliable authenticated tooling stack for local work, GitHub, Supabase, Vercel, CI evidence, and rendered verification, with current identity/scope and unavoidable human dependencies recorded from sanitized read-only proof.

**Owner:** Codex `/root`; the explicitly identified chat speaker and selected checkpoint is Ahmed

**Reviewer:** Ahmed is the ordinary checkpoint; D-22 standing Ahmed-and-Ziad authorization covers selected-task non-financial protected delivery, while fresh Ahmed-and-Ziad confirmation remains required immediately before real-money exposure

**Branch:** `wp00/tooling-auth-stack`

**Updated (UTC):** 2026-09-21T18:02:00Z

## Derived execution envelope

**Policy version:** 2

**Surfaces:** docs, tooling

**Risk:** R0 for the repository guidance and local read-only checks; any discovered material permission-boundary uncertainty widens before mutation

**Planning:** Minimal

**Model floor:** Luna Max

**Model runtime:** active model unverified and limitation reported; no model switch or shared configuration mutation is claimed

**Worker budget:** 0 used, maximum 1, nested workers prohibited

**Capabilities:** NONE

**Procedural skills:** trust-boundaries for the external identity/scope trust map; no additional skill activated

**Routing reason:** bounded revalidation and durable guidance of existing local, CLI, connector, and browser paths; no application authentication semantics or provider trust configuration is being redesigned

## Execution contract

**Dependencies:** WP00-T09 and its merged delivery evidence; `planning/agent-operability-audit.md` as a point-in-time baseline; current `AGENTS.md`, workflow, policy, runbook, package manifest, and task-record authority

**Inputs:** current Git remotes and credential-helper metadata; repository-pinned Supabase/Vercel/Playwright versions; read-only GitHub and Vercel connector results; safe CLI identity/project results; no tokens, private payloads, or browser credential state

**Files:** `docs/runbooks/poc-execution-runbook.md`; `scripts/show-work-state.ps1`; this task record; `planning/agent-operability-audit.md` only where current evidence supersedes the baseline; sanitized evidence under `evidence/wp00-pilot/`

**Verify:** local Git and pinned CLI read-only checks; minimum available GitHub/Vercel connector reads; `pwsh -NoProfile -File scripts/verify-agent-readiness.ps1`; `pwsh -NoProfile -File scripts/test-agent-handoff.ps1`; `corepack pnpm scan:secrets`; `git diff --check`; `git diff --stat`; full diff and changed-file scope review; exact-head required GitHub CI

**Pass:** every requested system has one evidence-backed classification and primary/fallback path; any human checkpoint is explicit and minimal; no duplicate integration or unnecessary browser/E2E check is introduced; post-completion work-state selects WP03-T04

**Evidence:** `evidence/wp00-pilot/2026-09-21_tooling-authentication-stack_local_5ad335d.md` plus any connector/CLI output summarized without secrets

**Rollback:** revert this task's repository commit; if a reversible local account-selection check is performed, restore the prior explicitly recorded active account; no provider-side mutation is authorized by this task

**Hard stop:** never print or inspect secrets, cookies, browser storage, signed URLs, passwords, PATs, service-role keys, database passwords, or credential payloads; stop before OAuth/consent, CAPTCHA, 2FA/OTP, new credentials, permission expansion, new external trust, billing, financial liability, provider/platform confirmation, or unresolved material security/authorization uncertainty

## Trust map

- **Authoritative identity and scope:** provider-enforced account/project/repository responses from the authenticated connector or official CLI, plus the local Git remote and repository-pinned project linkage; the chat speaker and service-account login do not prove founder identity.
- **Untrusted inputs:** branch names, repository/project strings, PR text, connector-rendered metadata before target validation, browser content, local environment names, and any requested scope supplied by a tool or page.
- **Recomputation point:** GitHub, Supabase, and Vercel enforce account, repository/project membership, permissions, and target scope server-side; the repository's guarded scripts remain the local policy boundary.
- **Allowed path:** read-only identity, metadata, project-link, deployment/status, and CLI-capability checks against the named UniMind targets.
- **Forbidden path:** credential extraction, wrong-target mutation, unreviewed identity switching during normal work, permission broadening, new integration authorization, paid operation, or treating a shared provider account as founder authorization.
- **Expiry/revocation/replay:** a session, account switch, connector reauthorization, project relink, or permission change invalidates earlier observations; re-probe once immediately before any consequential mutation and do not reuse stale identity evidence.
- **Exposure:** retain only account names, repository/project identifiers, permission classes, version/status results, run/check IDs, and sanitized limitations; never retain tokens, cookies, source text, provider payloads, or credential-bearing URLs.

## Steps

- [x] Read the current repository authority, baseline audit, task conventions, and clean main; select Ahmed and create the review branch.
- [x] Revalidate local Git, GitHub connector/CLI, pinned Supabase CLI, pinned Vercel CLI, and the minimum CI/tooling paths with the cheapest safe read-only checks.
- [x] Classify the requested systems and identify only demonstrated gaps or limitations.
- [x] Apply the smallest safe repository-guidance or local configuration corrections; do not add external integrations or credentials.
- [x] Handle and verify any genuine human-only checkpoint, one service at a time.
- [x] Record the current matrix, trust-boundary result, evidence, rollback, and rate-limit impact without inventing savings.
- [ ] Run selected proof, exact-head CI, protected delivery, affected proof if applicable, closure, and cleanup; confirm WP03-T04 is next.

## Handoff

**Changed:** Task registration, the post-foundation runbook entry, the current audit section, and the work-state routing label are recorded. No external service, hosted project, or credential has been mutated by the repository changes.

**Commands:** Intent routing passed for policy 2, docs/tooling, R0, minimal planning, Luna Max floor with unverified runtime disclosure. Local/Git, GitHub CLI, Supabase CLI, Vercel CLI, Playwright version, secret scan, readiness, and base CI read-only checks passed as recorded in the evidence.

**Remaining:** Candidate exact-head CI, protected PR review/merge, affected proof if selected, main synchronization, cleanup, and final handoff. The current-thread GitHub connector remains unavailable as `Unknown tool`; `gh` is the deterministic delivery fallback.

**Next safe action:** Commit the sanitized evidence, run the selected repository proof, push the candidate through the protected PR path, and request independent review if branch protection requires it; no further credential or hosted-project setup is needed.

**Reviewer action:** NONE for current non-financial work; GitHub and Supabase login checkpoints are complete and verified read-only. Fresh Ahmed-and-Ziad confirmation remains required only for real-money exposure.

## Revalidation receipt — 2026-09-21

- **Local/Git:** `corepack pnpm exec node --version` is `v24.19.0`; `corepack pnpm --version` is `10.34.5`; Git Credential Manager `2.5.0` is configured; the named `origin` remote resolves; `git ls-remote origin HEAD` and `git push --dry-run origin HEAD:refs/heads/wp00/tooling-auth-stack` passed without creating a remote branch.
- **GitHub connector:** the original in-thread probe identified `zeyadheheme-oss` with `pull: true` and `push: false`; after reconnection, the working chat returned sanitized identity/scope evidence for `unimind989-sys` on `UniMind-Project` with `pull: true`, `push: true`, `admin: true`, and `maintain: true`. Current-thread GitHub calls remain unavailable as `Unknown tool`.
- **GitHub CLI:** the human completed `gh auth login --hostname github.com --git-protocol https --web`; one redacted read-only verification reports active `unimind989-sys` in the OS keyring with HTTPS and `gist`, `read:org`, `repo`, and `workflow` scopes. `gh repo view` reports `unimind989-sys/UniMind-Project`, default branch `main`, public visibility, and `ADMIN` viewer permission. No token was printed or inspected; no second CLI account is configured.
- **Supabase CLI:** from the repository root, `corepack pnpm --version` is `10.34.5`, `corepack pnpm exec node --version` is `v24.19.0`, and pinned Supabase is `2.115.0`; after official login, `supabase projects list` exits 0 and lists `unimind-preview` as `ACTIVE_HEALTHY` and `unimind-beta` as `INACTIVE`. The checkout is not linked to a hosted ref, which is preserved by policy. The first version attempt hit a transient local telemetry-file rename race and was not treated as a version result. The earlier System32 login attempt failed before Supabase because Corepack looked for missing global `pnpm 12.5.1`; no credential was entered then.
- **Vercel CLI:** pinned `59.9.1` identifies `unimind989-sys`, team `unimind2`, and linked project `unimind-preview`; project inspection and one deployment list/inspection passed with `READY` status. The Vercel connector reports `not connected`.
- **Playwright:** pinned Test `1.62.1` version probe passed; the specialist CLI was not invoked because the current task does not select tracing, locator discovery, or test debugging.
- **Trust result:** allowed proof exists for read-only identity/scope/project/deployment inspection and Git dry-run transport; forbidden paths remain credential extraction, wrong-target mutation, scope broadening, new integration authorization, paid operations, and treating shared service identity as founder authorization.
- **Post-checkpoint connector result:** a fresh attempt to call each GitHub identity/scope/read endpoint returned the Codex runtime error `Unknown tool` before reaching GitHub. The supplied screenshot shows the GitHub app UI as `Connected` with `Interactive, Write` capabilities, but it does not expose the account or repository scope and cannot substitute for the read-only connector proof. This is recorded as an unavailable connector runtime, not as successful reauthorization or a new permission result.
- **Thread-local limitation:** Ahmed reports that the same GitHub connector works in a separate chat. That corroborates a stale binding in this task thread, but it is not itself proof of this task's actor/scope; only a sanitized structured result from the working chat can close the identity/scope check.
- **Sanitized structured result:** the working chat returned login `unimind989-sys`, repository `UniMind-Project`, installed account `unimind989-sys`, and `pull: true`, `push: true`, `admin: true`, `maintain: true`. No token, cookie, raw URL, or credential payload was transferred.
- **GitHub fallback result:** the pinned `gh` fallback is now authenticated and target-scoped as `unimind989-sys`; the independent `aboayman-oss` review identity remains a separate browser/human path and was not added to the CLI.
- **Supabase result:** the pinned CLI is authenticated to the approved service identity and can enumerate both intended hosted projects without linking or mutating either environment. No Supabase MCP is retained or needed.
- **Rate-limit result:** no measured token/credit savings are claimed. The task avoided browser duplication, a second GitHub/Supabase/Vercel MCP, E2E, Playwright CLI debugging, and raw deployment-log retrieval because the current acceptance criteria do not select them.
