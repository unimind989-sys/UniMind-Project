# Task record: WP00-T09 autonomous agent execution

**Task ID:** WP00-T09

**Status:** [x]

**Outcome:** A selected UniMind runbook task can move from compact deterministic routing through focused proof, exact-head protected delivery, affected production verification, closure, and cleanup without routine human orchestration or duplicated work.

**Owner:** Codex `/root`; Ahmed is the requester and selected chat speaker

**Reviewer:** Ahmed's WP00-T09 directive is the standing checkpoint and relays the task-scoped non-financial delivery authorization; fresh Ahmed-and-Ziad confirmation remains required immediately before real-money exposure

**Branch:** implementation `wp00/autonomous-agent-execution` (merged by PR #35 and deleted); corrective/closure `wp00/autonomous-agent-execution-closure`

**Updated (UTC):** 2026-09-21T15:19:29Z

## Derived execution envelope

**Policy version:** 2 (external-audit correction)

**Surfaces:** docs, tooling

**Risk:** R0 for repository implementation; terminal protected-main mutation is delivery R3 and cannot weaken branch protection

**Planning:** Intent pass was Deliberate for architecture uncertainty; the corrected actual-diff pass is Minimal for repository-only docs/tooling, with protected delivery planning at merge

**Model floor:** Sol High for WP00-T09 because the intent pass and sticky escalation retain the higher floor

**Model runtime:** The live primary model is not exposed to repository tooling, so the router reports `unverified` and the required floor before implementation; no shared configuration mutation or model-switch claim is permitted. A switch request is valid only when the runtime can verify an active model below the floor.

**Worker budget:** 0 used; maximum 1; nested workers prohibited

**Capabilities:** none for the final docs/tooling surface; the policy module under implementation is not misreported as an activated surface capability

**Procedural skills:** diagnosing-bugs, codebase-design, trust-boundaries, skill-maintainer, writing-for-agents

**Routing reason:** The implementation changes machine-consumed agent workflow and verification infrastructure but no product runtime; exact-head merge is a protected production-state mutation handled by terminal delivery.

## Execution contract

**Dependencies:** Reviewed WP00-T08 mock-only gate; completed WP01 and WP02 package gates; completed WP03-T01 through WP03-T03 evidence. This maintenance task does not reopen them and must return normal selection to WP03-T04.

**Inputs:** WP00-T09 directive; master-plan authority and D-22; runbook sections 0 and 3; current `AGENTS.md`, agent workflow, task/evidence templates, repo-scoped skills, work-state/readiness scripts, package commands, completed WP02/WP03 task records, and sanitized evidence.

**Files:** Versioned execution policy under `docs/agents/`; one TypeScript policy module plus derive/verify adapters under `scripts/`; focused fixtures/tests; compact frontend and trust-boundary capability guidance; affected task/evidence templates, work-state/readiness scripts, agent/skill documentation, D-22/master-plan/runbook synchronization, and WP00 evidence.

**Verify:** Focused policy unit tests; `pnpm verify:agent-policy`; repository skill validator; agent readiness; isolated handoff rehearsal because task discovery/resume changes; `pnpm verify` because verification/control-plane scripts change; `git diff --check`; `git diff --stat`; full diff and secret/scope scan; exact-head required GitHub CI.

**Pass:** Every WP00-T09 acceptance item is represented by executable policy, focused regression evidence, or an authoritative pointer; core rules are ENFORCED only after historical replay; conditional CI stays SHADOW; terminal delivery preserves branch protection and the fresh real-money boundary; merged clean `main` recommends WP03-T04.

**Evidence:** `evidence/wp00-pilot/2026-09-21_autonomous-agent-execution_local_ff15002.md`, `evidence/wp00-pilot/agent-execution-receipt.json`, and the final protected-delivery result appended to the report.

**Rollback:** Revert the WP00-T09 merge to restore the previous instruction/work-state/finalize behavior; keep broad CI unchanged throughout so conservative exact-head protection remains available.

**Hard stop:** Do not weaken a runbook requirement, protected security boundary, branch protection, evidence-to-state binding, privacy rule, or real-money confirmation; do not expose secrets/private data; do not implement WP03-T04; do not claim a model switch the runtime cannot prove.

## External-audit corrections

- Unknown paths widen conservatively only during the actual-diff pass; intent remains semantic. Known `workers/` paths classify as runtime instead of losing precision.
- Conditional CI remains `SHADOW` and broad CI remains unchanged. The router emits job predictions, the evidence assessor compares them with the same broad run, READY requires explicit regression plus run/would-skip evidence with zero unsafe contradictions, and fallback is per contradicted job. ENFORCED still requires a governed policy and CI workflow change.
- Model-floor output distinguishes verified satisfaction, verified insufficiency, and an unverified active model; only verified insufficiency requests a switch.
- Protected GitHub delivery probes structured connector and `gh` identity mechanics before browser use. Platform-mandated browser confirmation is recorded as a runtime limitation rather than zero-human completion.

### GitHub delivery trust map

- **Authoritative identity and scope:** GitHub's authenticated connector/CLI account and repository permission response; when those paths cannot select roles, the signed-in GitHub account shown by the side browser and GitHub's server-enforced PR/review state.
- **Untrusted inputs:** branch/PR text, requested role labels, browser-rendered content before account revalidation, and shared service-account identity as proof of which founder acted.
- **Recomputation point:** GitHub enforces repository permissions, author/reviewer separation, required checks, branch protection, and merge eligibility server-side.
- **Allowed path:** owner `unimind989-sys` owns/merges the protected PR and collaborator `aboayman-oss` approves the exact head after inspection.
- **Forbidden path:** PR-author self-approval, bypassing branch protection, or treating a shared login as founder authorization.
- **Expiry/replay/stale state:** re-probe identity and permissions immediately before mutation; anchor review and merge to the exact head; a session/account switch invalidates earlier identity observations.
- **Exposure:** inspect no token, cookie, password, browser storage, signed URL, or credential payload; retain only account role, PR/check IDs, and sanitized outcomes.

## Steps

- [x] Inspect the repository, governing workflow, relevant skills, runtime/tool access, and current clean main; register WP00-T09 on its review branch.
- [x] Implement the central policy in shadow and establish failing historical regression tests.
- [x] Correct the policy from regression evidence, validate it, and promote proven core rules to ENFORCED while conditional CI remains SHADOW.
- [x] Add trust-boundaries and narrowly repair TDD, diagnosis, codebase-design, image-to-code, Impeccable, and finalization conflicts.
- [x] Integrate routing/evidence metadata with task records and work-state output without rewriting completed records.
- [x] Slim and synchronize agent guidance, master-plan/D-22 authorization, runbook, README, CONTRIBUTING, templates, and skill governance.
- [x] Run focused and broad verification once against the stable candidate, review scope/secrets, and assemble sanitized evidence.
- [x] Reproduce the audited actual-diff and conditional-CI gaps with a focused red-capable regression loop.
- [x] Implement policy v2 unknown-path fallback, precise worker routing, CI predictions/evidence assessment, model-runtime actions, and accurate GitHub capability/confirmation guidance.
- [x] Run the invalidated focused and broad proof, bind sanitized evidence to the corrective candidate, then prepare one final protected closure and confirm WP03-T04 selection. These closure markers become authoritative only when this exact candidate reaches protected `main`.

## Handoff

**Changed:** Added the versioned policy, deterministic router, evidence assessment, historical regression suite, compact capability guidance, skill repairs, synchronized workflow/authorization guidance, and deterministic handoff selection proof.

**Commands:** Node 24.19.0/pnpm 10.34.5 focused policy replay passed 10 historical cases and 23 focused tests; all 22 skills validated; readiness passed 183 names, 46 links, 23 decisions, and 103 task contracts; the isolated handoff rehearsal passed including simulated WP03-T04 selection. Final `pnpm verify` passed formatting, lint, strict types, boundaries, 24-migration SQL policy, CI policy, policy replay, the 896-file secret scan, 330 unit, 15 integration with 2 intentional hosted skips, 22 security, 3 evaluation plus 3 synthetic foundation cases, 5 load-contract, 19 E2E, the safe production build, and client-artifact scanning. The first broad attempt rejected one lint warning; runtime planning/model/risk validation corrected it. Evidence assembly exposed and corrected receipt comparison against the wrong Git base and original task surfaces; all invalidated proof reran on implementation SHA `ff15002`. Exact-head GitHub Actions run `35590667885` passed dependency-audit, application, and disposable database CI for `385d3f5`; `aboayman-oss` approved that exact head; owner `unimind989-sys` merged PR #35 as `6c5cad8`; synchronized `origin/main` contains the reviewed candidate; the merged implementation branch was deleted locally and remotely. The audit regression loop first failed 4/26 focused cases at the real router seam. Policy v2 then passed 30/30 focused tests, 10 historical cases, 5 named conditional-CI regressions, strict types, all 22 skill validations, and readiness with 184 names, 46 links, 23 decisions, and 103 task contracts. PR #35's broad CI outcomes were reused once as shadow evidence: all three jobs remain SHADOW because each still lacks either run or would-skip observation coverage, with zero contradictions. Clean corrective candidate `8a28622` passed the invalidated `pnpm verify` gate with 898-file secret scanning, 337 unit, 15 integration plus 2 intentional hosted skips, 22 security, evaluation/load, 19 E2E, safe build, and client-artifact scan. Its isolated handoff rehearsal selected WP00-T09 now and WP03-T04 only after simulated closure.

**Remaining:** No implementation or local-proof work. Protected exact-head CI, independent review, merge, synchronized-main proof, and branch cleanup remain delivery conditions for this closure candidate.

**Next safe action:** Publish this exact closure candidate through one protected corrective pull request, then prove synchronized `main` selects WP03-T04. Do not implement WP03-T04.

**Reviewer action:** NONE for routine work; the WP00-T09 directive authorizes autonomous non-financial completion. Fresh Ahmed-and-Ziad confirmation is required only immediately before any real-money exposure.
